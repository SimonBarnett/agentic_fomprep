# Hostile MRB — shell compile/install v2 skeleton

**Date:** 2026-09-19  
**Repo:** SimonBarnett/agentic_fomprep  
**Scope reviewed:** v2 shell compile + install WP0 skeleton vs `docs/feature-request-shell-compile-install-v2-spec-0.1.md` (+ PDF)  
**Machine:** ionos  
**v1:** `src\Prepare-NamedForm.ps1` — no diff  

## Verdict

**PASS-with-nits** for the locked WP0 skeleton slice. **Not** ready for human UAT of live compile/install.

The parked FR and the job lock say: until `PinComplete`, skeleton only (parser, allowlist, WhatIf, schemas, refuse paths, SKILL.md, WP0 fail-fast). No guessed procedure/ENAME names. That slice is present on this tree and WP0-T* is green. Proof-instance compile/install (§15) is still red-by-design because pins are empty.

Only Bob may declare **ready for human UAT**. This board does not.

## Blockers

None against the skeleton lock.

These would be blockers if this push claimed WCF compile/install or `ok=true` compiled/installed:

- `PinComplete` is false; Prepare Upgrade / Install Upgrade ENAMEs are empty.
- No WCF walker. Runners refuse with `reason=pin_incomplete`, `wcfAttempted=false`, exit 2.
- `PRIORITY_WP0_INSTANCE` unset on ionos; WP0-R1..R7 skipped.

## Evidence (run on ionos)

| Gate | Result |
|------|--------|
| `v2/tools/Test-WP0.ps1` | exit 0, all WP0-T* PASS, R* skipped |
| `tools/Test-Pack.ps1` (now starts with WP0) | Test-Pack PASS (WP0 + AT6 + unit-* + AT3) |
| `git diff -- src/Prepare-NamedForm.ps1` | empty |
| `v2/tests/wp0-last.json` | `failed=0`, `pinComplete=false` |

WP0-T8 covered: unknown instance, live refuse, missing shell, DBI without flag, `..` path, catalog/plugin fixtures live-install refuse, WhatIf exit 0. All exit 2 on refuse; `errors[]` non-empty; no WCF.

WP0-T7: fixture `1042-sanitized.sh` parses to revision `1042` and codes `TAKESINGLEENT`, `TAKEENTHEADER`, `TAKEPROCSTEP`.

WP0-T6: product runners/MCP servers contain no hardcoded `PREPAREUPGRADE` / `INSTALLUPGRADE` / `PREPUPG` / `INSTUPG` tokens.

## Spec MUST / MUST NOT

| Requirement | Status |
|-------------|--------|
| v2 only; do not change repo-root `src\Prepare-NamedForm.ps1` | Met |
| Two catalog folders `priority-shell-compile` / `priority-shell-install` | Met |
| Two tools `compile_shell` / `install_shell` — never one combined tool | Met (sibling plugins; formprep MCP untouched) |
| Do not rebuild formprep or catalog MCP handlers | Met (`v2/apps/mcp-catalog/lib/mcp.ts` untouched; `priority-formprep` plugin untouched) |
| Shared lib allowlist, sql, cred, parse-sh | Met (`v2/lib/` + portable copies) |
| Result JSON aligned with FormPrep v2 | Met (ok, runId, instanceId, skill, executor, timestamps, reason, exitCode, errors[]) |
| Until PinComplete: no guessed ENAMEs / no WCF success path | Met |
| `allow_dbi` default false; DBI refuse before WCF | Met |
| Path allowlist: `..`, foreign UNC, catalog fixtures | Met |
| Catalog MCP stays grab-only | Met (no handler tools added on Amplify) |
| Red before WCF | Met |

## Required fixes (ordered) — next slice, not this skeleton

1. Human recon on a proof instance (`PRIORITY_WP0_INSTANCE`). Fill `v2/config/pin.json` + `pin.psd1` from dictionary titles. Do **not** invent ENAMEs. Flip `PinComplete` only after reading recon.
2. Implement WCF walk for Prepare Upgrade / Install Upgrade **from pins only**. File-step vs WINRUN is a pin (`WcfFileStepWorks`).
3. Install SQL gate from pinned install-log table; `TAKESINGLEENT` names must exist in `T$EXEC`; `postInstall.formsUnprepared[]` handoff to `prepare_form` (do not auto-prep).
4. Re-run WP0-R* on the proof instance. Fail on contradiction (R7).
5. Off-instance pack stays green. Then proof-instance ATs in FR §15. Then Bob UAT.

## Nits (do not block skeleton)

1. `v2/tools/parse-check.ps1` still hardcodes `M:\py\agentic_fomprep\...` and does not list the new scripts. `Test-WP0` parse-checks v2 `*.ps1` instead.
2. Shared lib is copied into four runner trees (same pattern as formprep). Drift risk if someone edits one copy.
3. Landing page `grok mcp add` example still uses the name `priority-formprep` for the catalog URL (grab-only host). Skills list is dynamic.
4. Fail-closed WCF path uses `reason=pin_incomplete` even if a later commit sets `PinComplete=true` without shipping a walker. That is safer than guessing; name the reason when the walker lands.
5. Amplify/DNS for `mcp-priority.ntsa.uk` was not redeployed or probed in this MRB. Catalog folders will show after the next host build; no MCP handler change is required.

## What this push is not

- Not a live compile of a Version Revision.
- Not a live install of a `.sh`.
- Not a pin of Prepare Upgrade / Install Upgrade ENAMEs.
- Not a v1 change.

Slogan still holds: **red before WCF**.
