# Grok plugin — priority-formprep (execute)

Local MCP tools: `list_instances`, `prepare_form`, `get_last_result`, `get_skill`.

Allowlist: `%USERPROFILE%\.priority-formprep\instances.json` (see `scripts/instances.example.json`).

Runner: `scripts/Prepare-NamedForm.ps1` (not repo-root `src\Prepare-NamedForm.ps1`).

```powershell
cd scripts
npm install
```
